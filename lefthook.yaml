pre-commit:
  jobs:
    - name: check formatting
      glob: "**/*.rs"
      run: cargo fmt --all -- --check

    - name: check clippy
      glob: "**/*.rs"
      run: cargo clippy --all

prepare-commit-msg:
  jobs:
    - name: lint commit message
      run: commitfmt
